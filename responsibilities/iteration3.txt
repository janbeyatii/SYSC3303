Jan - Independent Drone Execution
* Allow each drone to operate independently as its own process — DroneMain (app.DroneMain), one process per drone.
* Implement individual drone state machines running concurrently — DroneSubsystem unchanged; each process runs its own state machine via channel.
* Ensure each drone reports its position and status to the scheduler — All reporting through IDroneSchedulerChannel (updateDroneState, reportArrival, reportCompletion, reportReturnToBase).
* Handle drone task execution including travel, agent deployment, and return — No change; still in DroneSubsystem.
* Maintain communication with the scheduler using UDP messages — UDPDroneChannel; DronePacketBuilder and DronePacketParser (fireincident.udp) for message format.
* Channel abstraction so drone can use in-process or UDP — IDroneSchedulerChannel, InProcessDroneChannel; DroneSubsystem refactored to use channel.

Iman
 * Replace direct method calls with message based communication
 * Ensure Fire Incident Subsystem, Scheduler, and Drone Subsystem communicate exclusively through UDP
 * Implement packet creation and parsing for system messages
 * Handle sending and receiving packets reliably

Samy
* Create test scenarios involving multiple drones and fire events
* Verify communication between distributed processes
* Ensure scheduler correctly assigns drones to fire zones
* Update project documentation and testing instructions

Mithushan
* Extend the Scheduler to support multiple drones
* Track the status and location of each drone
* Assign fire incidents to the most appropriate available drone
* Balance workload between drones to maximize throughput
* Ensure scheduler updates drone assignments based on queue state